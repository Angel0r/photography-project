# ===== MinIO (console login; change in your real .env) =====
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=admin12345
MINIO_BUCKET=photo-thumbs

# ===== Optional: ClickHouse client vars (use when you enable auth later) =====
CH_HOST=clickhouse
CH_PORT=8123
CH_USER=app
CH_PASSWORD=changeme
CH_DATABASE=photos

# ===== Optional: CIFS (Unraid) — used if/when you add the SMB volume =====
UNRAID_USER=your_unraid_username
UNRAID_PASS=your_unraid_password
# Note: if your password contains a comma (,), use a credentials file instead of inline CIFS options.

# ===== Misc =====
TZ=Europe/Stockholm
